Функции ReadBMP() и WriteBMP()

Деление изображения на квадраты RECT

Случайное распределение квадратов по потокам

Само размытие (внутри BlurSquare)

Повторное размытие в цикле (для увеличения времени)

Опционально — привязка потоков к ядрам через SetThreadAffinityMask()